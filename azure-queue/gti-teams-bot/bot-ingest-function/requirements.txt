# =============================================================================
# GTI MS Teams Bot (Agentic) — Ingest Function — Dependencies
# =============================================================================

# Azure Functions Python worker (v2 programming model) — see function_app.py.
azure-functions==1.21.0

# HTTP client for the Bot Framework Connector API — posting the "looking
# into that…" placeholder (app/teams/bot_client.py, app/teams/context.py).
requests==2.31.0

# User-Assigned Managed Identity authentication for this function's own Bot
# Framework Connector token (app/teams/bot_client.py) — no client secret,
# ever.
azure-identity==1.17.0

# Hands a parsed activity off to the Worker Function App: sent as a plain
# UTF-8 text message via the SDK directly (matching this codebase's existing
# convention of using the storage SDKs directly rather than WebJobs bindings)
# rather than a queue_output binding, so this app needs no extensionBundle.
azure-storage-queue==12.9.0

# Type-safe, validated configuration from environment variables (Azure
# Function App Application Settings are exposed as env vars at runtime).
pydantic-settings==2.0

# Local development only — load .env file into environment when running a
# script directly outside of `func start` (which reads local.settings.json
# instead).
python-dotenv==1.0

# JWT decoding and key verification for Bot Framework security validation
# (app/teams/auth.py) — [crypto] pulls in `cryptography`, required for RS256.
pyjwt[crypto]==2.8.0
