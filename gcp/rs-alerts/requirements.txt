# =============================================================================
# RS Alerts (GTI Alerts -> Microsoft Teams) — GCP Cloud Run Functions Dependencies
# =============================================================================

# Google Cloud Functions Framework — provides HTTP trigger support for Cloud Run functions
functions-framework>=3.8.0

# HTTP client for the GTI Alerts API and the Teams incoming webhook
requests>=2.31.0

# Google Cloud Firestore — durable persistence for incremental alert cursor
google-cloud-firestore>=2.14.0

# Google Cloud Logging — structured log integration
google-cloud-logging>=3.11.0

# Type-safe, validated configuration from environment variables / Secret Manager
pydantic-settings>=2.0

# Local development only — load .env file into environment
python-dotenv>=1.0

# Flask for Functions Framework request/response handling
flask>=3.0.0
