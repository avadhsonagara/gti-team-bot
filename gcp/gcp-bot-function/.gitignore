# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environments
.venv/
venv/
ENV/
env.bak/
venv.bak/

# Environment variables & secrets
.env
.env.local
cert.key
cert.cer
fullchain.cer
# GCP service account key files (e.g. gcloud iam service-accounts keys
# create) — local dev only, never commit. teams-app-manifest/*.json (the
# Teams app manifest) is explicitly re-included since it isn't a secret.
*.json
!teams-app-manifest/*.json

# IDEs & OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db
